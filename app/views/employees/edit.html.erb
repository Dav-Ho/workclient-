<%= form_tag("/employees") do %>

<div>
  <%= label_tag(:first_name) %>
  <%= text_field_tag(:first_name, @employee['first_name']) %>
</div>

<div>
  <%= label_tag(:last_name) %>
  <%= text_field_tag(:last_name, @employee['last_name']) %>
</div>

<div>
  <%= label_tag(:email) %>
  <%= text_field_tag(:email, @employee['email']) %>
</div>

<%= submit_tag('Create Employees') %>
<% end %>
